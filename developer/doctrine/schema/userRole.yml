UserRole:
  columns:
    userId:
      type: integer
      primary: true
    roleId:
      type: integer
      primary: true
  relations:
    User:
      local: userId
      foreign: userId
    Role:
      local: roleId
      foreign: roleId

  options:
    type: INNODB
    collate: utf8_bin
    charset: utf8